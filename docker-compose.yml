version: '3.8'

services:
  spring:
    image: moodtrbl/dev-order-agent-service:latest
    container_name: order-agent-service
    ports:
      - "8081:8081"
    environment:
      - SPRING_PROFILES_ACTIVE=deploy
      - HOSTNAME=order-agent-service